Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
redis==5.0.8

# Production serving
whitenoise==6.7.0
dj-database-url==2.2.0
psycopg2-binary==2.9.9
gunicorn==22.0.0
uvicorn[standard]==0.30.1

# Security
django-ratelimit==4.1.0

# AI Keys — lightweight clients only (no torch/transformers on free tier)
openai==1.54.3
google-generativeai==0.8.3
python-dotenv==1.0.1

# Mem0 (lightweight API client)
mem0ai==0.0.11

# Utilities
psutil==6.1.0
pydantic==2.10.2

# ── Heavy ML packages below are disabled on Render free tier (512MB RAM limit) ──
# sentence-transformers==3.2.1  # pulls in torch ~1GB — enable on paid tier
# langchain==0.3.7
# langchain-openai==0.2.8
# langchain-community==0.3.7
# tiktoken==0.8.0
# numpy==1.26.4
# pandas==2.2.3
